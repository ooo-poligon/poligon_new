<% @content.each do |c| %>
  <% if c.page == params[:id] %>
  <% generate_meta_from c.title %>
    <h1><%= c.title %></h1>
    <div>
      <%= c.content.gsub('href="/PDF', 'href="/PDF')
                   .gsub('src="/images', 'src="/images')
                   .html_safe %>
    </div>
  <% end %>
<% end %>
