<%= render "shared/breadcrumb_search", locals: {for: "publications", item: @video} %>
<p id="notice"><%= notice %></p>
<br>
<% title = @video.title %>
<% generate_meta_from title %>
<h1><%= title %></h1>
<p>
  <%= @video.content.html_safe
      .gsub('href="/PDF', 'href="/PDF')
      .gsub('src="/js', 'src="/js')
      .gsub('src="/images', 'src="/images')
      .html_safe %>
</p>
<%= render partial: 'shared/videos_list', locals: { videos: @videos } %>

<%= render 'shared/article_footer', locals: { item: @video } %>
