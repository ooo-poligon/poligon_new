<tr class="success">
  <td style="text-align:left">
    Базовая:
  </td>
  <td style="text-align:left">
  </td>
  <td style="color: blue; text-align:right;">
  <button class="clipboard-btn btn btn-xs btn-outline-primary copy-btn" data-clipboard-text="<%= article_text %><%= product.title %> — Базовая цена: <%= get_prices_eur(product)[0].round(2) %> евро c НДС. <%= information_on_the_availability %>">
    &euro;<%= get_prices_eur(product)[0].round(2) %>
    </button>
  </td>
  <td style="color: green; text-align:right; ">
  <button class="clipboard-btn btn btn-xs btn-outline-primary copy-btn" data-clipboard-text="<%= article_text %><%= product.title %> — Базовая цена: <%= get_prices_rub(product)[0].round(2) %> рублей c НДС. <%= information_on_the_availability %>">
    <%= get_prices_rub(product)[0].round(2) %>р.
    </button>
  </td>
  <td width="25%"style="color: brown; text-align:right; ">
  <button class="clipboard-btn btn btn-xs btn-outline-primary copy-btn" data-clipboard-text="<%= article_text %><%= product.title %> — Базовая цена: <%= (get_prices_rub(product)[0] / 1.20).round(2) %> рублей без НДС. <%= information_on_the_availability %>">
    <%= (get_prices_rub(product)[0] / 1.20).round(2) %>р.
    </button>
  </td>
</tr>